const choices = ['rock', 'paper', 'scissors'];
function getComputerChoice() {
    choices[Math.floor(Math.random()*choices.length)]
}

const playerSelection = pepper
const computerSelection = getComputerChoice();